# backend/app/schemas/base_schemas.py
from pydantic import BaseModel

# No schemas needed here; task and tenant schemas are in task.py and tenant.py
# This file can be removed if unused, or kept empty for future shared schemas